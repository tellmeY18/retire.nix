# secrets.yaml — Encrypted Helm values for the cloudnative-pg operator chart.
#
# !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
# THIS FILE MUST BE SOPS-ENCRYPTED BEFORE COMMITTING.
# Run: sops --encrypt --in-place k8s/apps/cnpg-operator/secrets.yaml
# !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
#
# The base CNPG operator installation from the public chart does NOT require
# any secrets.  This file is a placeholder so the helmfile `secrets://` entry
# is valid.  Add values here only if you need to pull from a private registry
# or supply other operator-level credentials.
#
# Example structure (encrypt this before committing):
#
# imageCredentials:
#   registry: "registry.example.com"
#   username: "YOUR-REGISTRY-USERNAME"
#   password: "YOUR-REGISTRY-PASSWORD"
#
# Currently no secrets are configured — leave this file as an empty map.
{}
