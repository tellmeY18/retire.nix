# secrets.yaml — sops-encrypted Helm values for the rustfs-operator chart.
#
# This file is managed by sops. Edit with:
#   just k8s::edit-secret apps/rustfs-operator/secrets.yaml
#
# Currently empty — the operator itself has no secrets.
# Placeholder kept for consistency with other apps.
{}
