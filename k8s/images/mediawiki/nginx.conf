worker_processes auto;
error_log /dev/stderr warn;
pid /tmp/nginx.pid;

events {
    worker_connections 512;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    access_log /dev/stdout;
    sendfile on;
    keepalive_timeout 65;
    client_max_body_size 100m;

    server {
        listen 80;
        server_name _;
        root /var/www/html;
        index index.php;

        # Static health check (no PHP needed)
        location = /health.txt {
            access_log off;
        }

        # Block sensitive paths
        location ~ ^/(images/deleted|cache|includes|maintenance)/ {
            return 403;
        }

        # PHP entry points with PATH_INFO support
        # Handles: rest.php/v1/search/title, api.php?action=..., index.php, healthcheck.php
        location ~ ^(?<script_name>.+\.php)(?<path_info>/.*)?$ {
            fastcgi_pass 127.0.0.1:9000;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$script_name;
            fastcgi_param PATH_INFO $path_info;
            include fastcgi_params;
        }

        # Short URLs — wiki article paths
        location / {
            try_files $uri $uri/ @mediawiki;
        }

        location @mediawiki {
            rewrite ^/(.*)$ /index.php?title=$1&$args last;
        }

        # Static assets — long cache
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
            expires 30d;
            access_log off;
        }

        # Images security header
        location /images/ {
            add_header X-Content-Type-Options nosniff;
        }
    }
}
