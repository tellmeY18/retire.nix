---
# Penpot namespace with Pod Security Admission.
# 'baseline' permits the runAsUser/fsGroup that Penpot's chart sets (uid 1001).
apiVersion: v1
kind: Namespace
metadata:
  name: penpot
  labels:
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/enforce-version: latest
