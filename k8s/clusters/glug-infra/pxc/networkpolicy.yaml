---
# networkpolicy.yaml — Default-deny + explicit allow NetworkPolicies for the
# pxc-clusters namespace.
#
# Defense in depth: even though the cluster is reachable only over the
# tailnet, NetworkPolicies prevent an accidentally-exposed pod from
# contacting the database without an explicit allow rule.
#
# Label reference (from `kubectl get pods --show-labels`):
#   PXC instances:     app.kubernetes.io/component=pxc, app.kubernetes.io/instance=mysql-pxc-db
#   HAProxy pods:      app.kubernetes.io/component=haproxy, app.kubernetes.io/instance=mysql-pxc-db
#
# NOTE: The Helm release name is `mysql`, but the pxc-db chart uses
# `<release>-pxc-db` as the full name, so the instance label is
# `mysql-pxc-db` (not just `mysql`).

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: pxc-clusters
  labels:
    app.kubernetes.io/part-of: percona-xtradb-cluster
    app.kubernetes.io/managed-by: kustomize
spec:
  podSelector: {}
  policyTypes:
    - Ingress
---
# Allow ingress to HAProxy pods from Tailscale ts-proxy and intra-namespace.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-haproxy-ingress
  namespace: pxc-clusters
  labels:
    app.kubernetes.io/part-of: percona-xtradb-cluster
    app.kubernetes.io/managed-by: kustomize
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/component: haproxy
      app.kubernetes.io/instance: mysql-pxc-db
  policyTypes:
    - Ingress
  ingress:
    # Rule 1: Client SQL traffic (3306, 3309) from tailscale, intra-namespace,
    #          pxc-system (operator smart-update), and monitoring (adminer/debug).
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: tailscale
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: pxc-clusters
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: pxc-system
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
        # Ghost CMS connects to HAProxy for MySQL access.
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ghost
        # MediaWiki connects to HAProxy for MySQL access.
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: mediawiki
      ports:
        - protocol: TCP
          port: 3306
        - protocol: TCP
          port: 3309
    # Rule 2: Prometheus scrapes HAProxy stats on port 33062.
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
      ports:
        - protocol: TCP
          port: 33062
---
# Allow ingress to PXC instance pods from HAProxy, operator, and intra-cluster.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-pxc-ingress
  namespace: pxc-clusters
  labels:
    app.kubernetes.io/part-of: percona-xtradb-cluster
    app.kubernetes.io/managed-by: kustomize
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/component: pxc
      app.kubernetes.io/instance: mysql-pxc-db
  policyTypes:
    - Ingress
  ingress:
    # Rule 1: MySQL traffic from HAProxy + operator + backup + intra-cluster.
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: haproxy
              app.kubernetes.io/instance: mysql-pxc-db
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: pxc-system
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: pxc
              app.kubernetes.io/instance: mysql-pxc-db
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
        # XtraBackup pods (scheduled backups + on-demand)
        - podSelector:
            matchLabels:
              percona.com/backup-type: xtrabackup
              app.kubernetes.io/instance: mysql-pxc-db
      ports:
        - protocol: TCP
          port: 3306
        - protocol: TCP
          port: 33060
        - protocol: TCP
          port: 33062
        # Galera replication ports
        - protocol: TCP
          port: 4567
        - protocol: TCP
          port: 4568
        - protocol: TCP
          port: 4444
        # mysqld_exporter metrics
        - protocol: TCP
          port: 9104
---
# Allow ingress to XtraBackup pods from PXC (SST streaming).
# garbd joins the Galera cluster via port 4567 on the PXC pod, then PXC
# streams the backup data BACK to the xtrabackup pod. Without this rule,
# the default-deny policy blocks the SST stream and garbd returns code 102.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-backup-ingress
  namespace: pxc-clusters
  labels:
    app.kubernetes.io/part-of: percona-xtradb-cluster
    app.kubernetes.io/managed-by: kustomize
spec:
  podSelector:
    matchLabels:
      percona.com/backup-type: xtrabackup
      app.kubernetes.io/instance: mysql-pxc-db
  policyTypes:
    - Ingress
  ingress:
    # PXC pods stream SST data back to the backup pod.
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: pxc
              app.kubernetes.io/instance: mysql-pxc-db
      ports:
        - protocol: TCP
          port: 4444
        - protocol: TCP
          port: 4567
        - protocol: TCP
          port: 4568
