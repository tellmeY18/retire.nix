# secrets/chopper/secrets.yaml
#
# THIS IS A PLACEHOLDER — not yet encrypted!
#
# To encrypt this file:
#   1. Generate host age key: ssh-to-age < /etc/ssh/ssh_host_ed25519_key.pub
#   2. Update .sops.yaml with the real key
#   3. Run: sops secrets/chopper/secrets.yaml
#
# Secrets managed here:
tailscale-auth-key: PLACEHOLDER_REPLACE_WITH_REAL_KEY
nextcloud-admin-pass: PLACEHOLDER_REPLACE_WITH_REAL_PASS
cloudflared-tunnel-credentials: |
    PLACEHOLDER_REPLACE_WITH_TUNNEL_JSON
